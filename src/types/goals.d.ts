type Goal = {
  id: string;
  title: string;
  description?: string;
  created_at?: string;
  currency: string;
  saved?: number;
  price: number;
  deadline?: string;
  label?: string;
  is_priority?: boolean;
};
