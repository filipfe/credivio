export default function MinutesRead({ content }: { content: string }) {
  return (
    <div className=" text-primary border border-primary text-sm py-1 px-3 rounded-full max-w-max">
      {content}
    </div>
  );
}
